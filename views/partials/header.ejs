<header>
    <h1>blog</h1>
    <nav>
        <a href="/">home</a>
        <% if(locals.session.user?.role === "admin") {%> 
            <a href="/admin">admin</a>
        <% } %> 
        <% if(locals.session.isLogged) { %>
            <a href="/entry/dashboard">dashboard</a>
            <a href="/entry/signout">sign out</a>
        <% } else { %>
            <a href="/entry">sign in/up</a>            
        <% } %> 
    </nav>
</header>